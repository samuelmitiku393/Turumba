{
  "name": "turumba",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/bot",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=apps/bot\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:bot": "npm run dev --workspace=apps/bot",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=server",
    "db:push": "prisma db push --schema=./prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "db:studio": "prisma studio --schema=./prisma/schema.prisma",
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "type-check": "npm run type-check --workspace=apps/web && npm run type-check --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
